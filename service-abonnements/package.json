{
  "name": "service-abonnements",
  "version": "1.0.0",
  "description": "Microservice de gestion des abonnements — Systeme de billetterie intelligente",
  "main": "src/server.js",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "seed:formules": "node src/seed/seedFormules.js",
    "test": "node --test --test-concurrency=1 \"tests/**/*.test.js\"",
    "test:unitaires": "node --test --test-concurrency=1 \"tests/unitaires/*.test.js\"",
    "test:api": "node --test --test-concurrency=1 \"tests/api/*.test.js\""
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.3",
    "mysql2": "^3.11.0",
    "sequelize": "^6.37.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.2",
    "supertest": "^7.2.2"
  }
}
