# Optionnel : force l'URL des 3 backends. Sans ces variables, le frontend
# deduit automatiquement l'hote a partir de celui utilise pour charger la
# page (window.location.hostname) - utile en local et en reseau sans rien
# configurer. A ne definir que pour un deploiement (domaine de production).
VITE_API_URL=
VITE_ABONNEMENTS_API_URL=
VITE_BILLETTERIE_API_URL=
