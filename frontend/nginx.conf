server {
  listen 80;
  root /usr/share/nginx/html;
  index index.html;

  # Application a page unique (React Router) : toute URL inconnue renvoie
  # index.html, et c'est React qui affiche la bonne page.
  location / {
    try_files $uri /index.html;
  }
}
